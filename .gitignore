# dependencies
node_modules/

# React build output
client/build/

# environment files
.env
.env.local
.env.*.local

# editor
.DS_Store
.vscode/

# Backups (local only)
backups/*.json
backups/*.dump
backups/*.sql

# Old flat-file data store (no longer used, but just in case)
chores.json
